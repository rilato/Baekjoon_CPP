#include <iostream>
#include <algorithm>

using namespace std;

int main() {
	int input;
	int num;

	cin >> input;

	int* arr = new int[input];

  // cin >> num; arr[i] = num;으로 나눠 쓰지 않고, 한번에 줄여서 쓸 수 있다.
	for (int i = 0; i < input; i++) {
		cin >> arr[i];
	}

	sort(arr, arr + input);

	for (int i = 0; i < input; i++)
		cout << arr[i] << '\n';

	delete[] arr;
}
