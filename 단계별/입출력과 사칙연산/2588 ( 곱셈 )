#include <iostream>
#include <string>

using namespace std;

int main() {
	string a, b;
	int num1, num2;

	cin >> a >> b;

	num1 = stoi(a);

	for (int i = 2; i >= 0; i--) {
		num2 = stoi(b.substr(i, 1));
		cout << num1 * num2 << endl;
	}

	num2 = stoi(b);
	cout << num1 * num2;
}
