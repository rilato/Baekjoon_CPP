#include <iostream>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int N, K;
	int num, k;

	cin >> N >> K;

	for (int i = 0; i < N; i++) {
		cin >> num >> k;
	}

	cout << "비와이";
}
