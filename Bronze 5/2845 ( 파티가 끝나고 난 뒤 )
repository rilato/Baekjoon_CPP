#include <iostream>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int num1, num2, product;
	int arr[5];

	cin >> num1 >> num2;

	product = num1 * num2;

	for (int i = 0; i < 5; i++) {
		cin >> arr[i];
	}

	for (int i = 0; i < 5; i++) {
		cout << arr[i] - product << ' ';
	}
}
